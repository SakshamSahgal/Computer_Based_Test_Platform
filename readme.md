Progress -

Index.html (HomePage) -

login as admin -

        saves session cookies
            {logged_in ,yes}
            {user_id,ID entered}
            {Name , name from database}
        redirects to Admin portal

AdminPortal.html

    register user
    logout button

RegisterUser.html
